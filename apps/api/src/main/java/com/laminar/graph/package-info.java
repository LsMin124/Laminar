/**
 * 읽기 전용 화면 집계 계층 (BFF) — DX-19.
 *
 * <p>여러 도메인을 가로질러 화면 단위 페이로드를 조립하는 곳: 그래프(카드+관계+그룹+카테고리), 캘린더(카드+날짜메모). 도메인 CRUD·쓰기 불변식은 각 도메인 패키지가
 * 소유하고, 여기는 읽기 조립만 한다 — 쓰기 서비스 호출 금지. 신규 화면 집계(장비 요약 등)도 여기에 추가한다.
 *
 * <p>도메인 리포지토리 원정 접근은 본 패키지(+cron·admin·rrule)에만 허용 — ArchitectureTest가 강제.
 */
package com.laminar.graph;
